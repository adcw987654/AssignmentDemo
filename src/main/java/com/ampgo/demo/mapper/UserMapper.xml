<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ampgo.demo.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.ampgo.demo.entity.UserEntity">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="account" property="account" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="last_login_time" property="lastLoginTime" jdbcType="TIMESTAMP"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, account, password, status, create_time, update_time, last_login_time
    </sql>
    <insert id="insert">
        INSERT INTO `USER` (account, password, status, create_time, update_time, last_login_time)
        VALUES (#{account}, #{password}, #{status},#{createTime}, #{updateTime}, #{lastLoginTime})
    </insert>

    <update id="updateLastLoginTime">
        UPDATE `USER`
        SET last_login_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateVerifyStatus">
        UPDATE `USER`
        SET status = #{status}
        WHERE account = #{account}
    </update>

    <select id="findByAccount" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `USER`
        WHERE account = #{account}
    </select>

    <select id="findById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `USER`
        WHERE id = #{id}
    </select>

    <select id="countByAccountOrEmail" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM `USER`
        WHERE account = #{account}
    </select>

</mapper>
